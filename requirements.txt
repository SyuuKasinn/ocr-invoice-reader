# PaddleOCR-VL 1.5 dependencies (paddlepaddle itself is installed by
# install.sh, which auto-picks CPU vs GPU build; see README for details).
paddleocr>=3.0.0
paddlex[ocr]>=3.5.0

# Image processing
opencv-python>=4.8.0,<5.0.0
numpy>=1.24.0,<2.1.0
Pillow>=10.0.0,<11.0.0

# Schemas
pydantic>=2.0.0,<3.0.0
